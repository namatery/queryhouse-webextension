# QueryHouse

QueryHouse is a Chrome-compatible MV3 web extension for local ClickHouse Play pages.

## Scripts

- `npm run dev` starts WXT development mode.
- `npm run build` creates a production extension build.
- `npm run typecheck` runs WXT preparation and TypeScript checks.
- `npm test` runs unit tests.
- `npm run lint` runs ESLint.
